   private void initData()
        {
            if (Formateurs.Any())
                return;

                var Fr = new Formateur()
                {
                    Nom= "Bill",
                   
                };
               Formateurs.Add(Fr);

                SaveChanges();
            
        }